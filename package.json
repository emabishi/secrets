{
  "name": "secrets",
  "version": "1.0.0",
  "description": "Jouranlling application",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm:start-server\" \"npm:start-client\" ",
    "start-server": "cd api && npm start ",
    "start-client": "cd client && npm start",
    "install": "cd api && npm install && cd client && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/emabishi/secrets.git"
  },
  "author": "@mabishi",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/emabishi/secrets/issues"
  },
  "homepage": "https://github.com/emabishi/secrets#readme",
  "devDependencies": {
    "concurrently": "^3.6.0"
  }
}
